services:
  mysql:
    container_name: maria-12.0
    image: mariadb:12.0
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --default-time-zone=Asia/Jakarta
      - --max_allowed_packet=64M
    environment:
      - MYSQL_ROOT_PASSWORD_FILE=/database-maria-12-0-root-password
    ports:
      - 33306:3306
    volumes:
      - ./data:/var/lib/mysql
      - ./.root-passwd:/database-maria-12-0-root-password:ro
    restart: unless-stopped

networks:
  default:
    external: true
    name: nser-docker
